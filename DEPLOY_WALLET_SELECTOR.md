# 部署自定义钱包选择器

## ✅ 完成的更新

### 主要改进

1. **自定义钱包选择器 UI** ✅
   - 美观的模态框设计
   - 只显示 MetaMask 和 WalletConnect
   - 不会触发 Trust Wallet 或其他钱包

2. **MetaMask 专用连接** ✅
   - 智能检测 MetaMask Provider
   - 避免多钱包冲突
   - 即使安装了多个钱包也能正确连接 MetaMask

3. **WalletConnect 占位符** ✅
   - UI 已准备好
   - 代码结构已搭建
   - 未来可以快速集成

4. **改进的用户体验** ✅
   - 清晰的视觉反馈
   - 流畅的动画效果
   - 友好的错误提示

## 🚀 立即部署

### 步骤 1: 提交代码

```bash
# 查看更改
git status

# 添加所有文件
git add .

# 提交
git commit -m "feat: implement custom wallet selector

Features:
- Custom wallet selector modal UI
- Only show MetaMask and WalletConnect options
- Prevent Trust Wallet/Phantom interference
- Smart MetaMask provider detection
- WalletConnect placeholder ready
- Improved user experience

Fixes:
- Trust Wallet popping up instead of MetaMask
- Multi-wallet conflicts
- Confusing wallet selection experience"

# 推送到 develop（需要输入密码）
git push origin develop
```

### 步骤 2: 等待 Vercel 部署

访问 Vercel Deployments 页面查看进度。

### 步骤 3: 测试

打开部署的 URL 并测试：

## 🧪 测试清单

### ✅ 基础功能测试

**1. 钱包选择器显示**
- [ ] 点击 "Connect Wallet"
- [ ] 看到弹出的模态框
- [ ] 模态框标题显示 "Connect Wallet"
- [ ] 看到两个选项：MetaMask 和 WalletConnect
- [ ] 模态框外观美观，动画流畅

**2. MetaMask 连接**
- [ ] 点击 MetaMask 选项
- [ ] 模态框关闭
- [ ] **只有 MetaMask 弹出连接请求**
- [ ] **Trust Wallet 不会弹出**
- [ ] **Phantom 不会弹出**
- [ ] 确认连接后，显示钱包地址

**3. 刷新页面**
- [ ] 连接 MetaMask 后刷新页面
- [ ] 自动恢复连接状态
- [ ] 显示钱包地址
- [ ] 余额正确加载

**4. 再次点击 Connect Wallet**
- [ ] 断开钱包
- [ ] 再次点击 "Connect Wallet"
- [ ] 钱包选择器弹出
- [ ] 选择 MetaMask
- [ ] **只弹出 MetaMask，不弹出其他钱包**

**5. WalletConnect（暂时）**
- [ ] 点击 WalletConnect 选项
- [ ] 看到提示 "WalletConnect support coming soon"
- [ ] 模态框关闭

**6. 关闭模态框**
- [ ] 点击 × 按钮可以关闭
- [ ] 点击背景遮罩可以关闭
- [ ] 按 ESC 键... （当前未实现）

### ✅ 购买功能测试

**7. 购买代币**
- [ ] 连接 MetaMask
- [ ] 选择 POWER 代币
- [ ] 输入数量
- [ ] 点击 "Buy"
- [ ] 不提示 "contract not configured"
- [ ] MetaMask 弹出交易确认
- [ ] 确认交易
- [ ] 交易成功
- [ ] 余额更新

### ✅ 多钱包环境测试

**8. 安装多个钱包**（重要！）

如果你有 MetaMask + Trust Wallet + Phantom：

- [ ] 点击 "Connect Wallet"
- [ ] 钱包选择器只显示 MetaMask 和 WalletConnect
- [ ] 选择 MetaMask
- [ ] **验证：只有 MetaMask 弹出，Trust Wallet 和 Phantom 保持静默**
- [ ] 连接成功

**9. 刷新后（多钱包环境）**
- [ ] 刷新页面
- [ ] 自动恢复 MetaMask 连接
- [ ] 再次点击 Connect
- [ ] **仍然只弹出 MetaMask**

## 📊 预期结果

### 成功的表现

✅ **钱包选择器**
```
用户点击 "Connect Wallet"
    ↓
显示自定义模态框
    ├── MetaMask 选项（带图标和说明）
    └── WalletConnect 选项（带图标和说明）
    ↓
用户选择 MetaMask
    ↓
只有 MetaMask 弹出连接请求
    ↓
其他钱包保持安静 ✓
```

✅ **控制台日志**
```javascript
// 点击 Connect Wallet
// 点击 MetaMask 选项
Connecting to MetaMask...
MetaMask connected: { address: "0x...", chainId: 11155420 }
Current chain: 11155420 Expected: 11155420
Wallet connected successfully
```

❌ **不应该看到的**
```
// 不应该有这些日志
Trust Wallet connecting...
Phantom connecting...
```

## 🐛 如果还有问题

### 问题 A: Trust Wallet 仍然弹出

**可能原因：**
- 浏览器缓存了旧代码
- Trust Wallet 扩展有特殊行为

**解决方案：**

1. **清除缓存并硬刷新**
```
Windows/Linux: Ctrl + Shift + R
Mac: Cmd + Shift + R
```

2. **检查代码是否正确部署**
- 在 Vercel 确认部署时间是最新的
- 检查部署日志是否有错误

3. **临时禁用 Trust Wallet 扩展**
- 在浏览器扩展管理中
- 禁用 Trust Wallet
- 刷新页面重试

### 问题 B: WalletConnect 显示错误

**这是正常的！**

WalletConnect 当前是占位符，显示 "support coming soon" 是预期行为。

### 问题 C: MetaMask 不弹出

**检查：**

1. **MetaMask 是否已安装？**
```javascript
// 在控制台执行
window.ethereum.isMetaMask
// 应该返回 true
```

2. **是否有其他钱包覆盖了 window.ethereum？**
```javascript
// 在控制台执行
window.ethereum.providers
// 查看所有 provider
```

3. **查看错误消息**
- 检查是否显示 "MetaMask not installed"
- 如果是，安装 MetaMask 扩展

### 问题 D: 购买时提示 contract not configured

**这不应该发生了！**

如果仍然出现：

1. **查看控制台日志**
```javascript
// 应该看到
Buy token - provider found, checking network...
Minter contract: 0x26F87856E62f2F72feD55938972684c2C1eFDcC9
```

2. **如果没有看到上述日志**
- 代码可能没有正确部署
- 清除缓存重试

## 📸 截图参考

### 钱包选择器应该长这样

```
┌─────────────────────────────────────────┐
│  Connect Wallet                      ×  │
├─────────────────────────────────────────┤
│                                         │
│  ┌───────────────────────────────────┐ │
│  │                                   │ │
│  │  🦊  MetaMask               →    │ │
│  │       Connect with MetaMask       │ │
│  │       extension                   │ │
│  │                                   │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │                                   │ │
│  │  🔗  WalletConnect          →    │ │
│  │       Scan with mobile wallet     │ │
│  │                                   │ │
│  └───────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

**视觉效果：**
- 白色圆角卡片
- 半透明黑色背景
- 平滑的淡入动画
- 悬停时卡片上移和边框高亮

## ✨ 新功能演示

### 之前 vs 现在

**之前：**
```
用户点击 Connect Wallet
    ↓
浏览器钱包选择器弹出
    ├── MetaMask
    ├── Trust Wallet    ← 不想要的
    ├── Phantom         ← 不想要的
    └── Coinbase Wallet ← 不想要的
    ↓
用户关闭选择器
    ↓
Trust Wallet 一直弹 ❌
```

**现在：**
```
用户点击 Connect Wallet
    ↓
我们的自定义选择器弹出
    ├── MetaMask    ← 支持 ✓
    └── WalletConnect ← 即将支持
    ↓
用户选择 MetaMask
    ↓
只有 MetaMask 弹出 ✅
其他钱包安静 ✅
```

## 🎯 成功标准

- [x] 构建成功
- [ ] 自定义钱包选择器显示
- [ ] 只弹出 MetaMask（不弹出 Trust Wallet）
- [ ] 连接成功
- [ ] 购买功能正常
- [ ] 刷新后自动恢复
- [ ] 美观的 UI 和动画

## 📞 需要帮助？

如果测试后仍有问题，请提供：

1. **浏览器信息**
   - 名称和版本
   - 已安装的钱包扩展列表

2. **控制台日志**
   - 打开开发者工具（F12）
   - 复制所有日志和错误

3. **截图**
   - 钱包选择器的截图
   - 错误提示的截图

4. **复现步骤**
   - 详细描述操作步骤
   - 预期行为 vs 实际行为

---

**版本：** v2.0.0 - Custom Wallet Selector
**构建状态：** ✅ 通过
**文档：** CUSTOM_WALLET_SELECTOR.md
